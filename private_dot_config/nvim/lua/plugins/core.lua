return {
  {
    "LazyVim/LazyVim",
    version = false,
    opts = {
      kind_filter = {
        -- See all symbols instead of default kind_filter by lazyvim
        default = false,
      },
    },
  },
}
